# Node 编码规范

约定 Node.js 运行环境、模块格式、错误处理与工程化脚本等写法。

- 与 [JavaScript 编码规范](/code/javascript) 共用 ESLint 规则时，可选用 Node 专用 `extends` 配置（规划中）。
- 本 monorepo 根目录使用 **pnpm workspaces** + **Lerna** 管理多包。

> 详细规则条目待补充。
