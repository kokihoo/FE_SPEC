# TypeScript 编码规范

TypeScript 规范在 JavaScript 规则之上扩展类型相关约定（`@typescript-eslint` 等），与 [JavaScript 编码规范](/code/javascript) 共用同一套 ESLint 接入流程。

## 说明

- 业务项目选型为 TS 时，在 `encode-fe-lint init` 中勾选对应能力即可生成配置模板。
- 本 monorepo 内 ESLint 共享包尚在规划中。

> 详细规则条目待补充。
